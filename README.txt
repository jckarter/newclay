Newclay is an experimental offshoot of the Clay programming language
(http://claylabs.com/clay/). The Newclay compiler requires Clay to compile.
Once Clay is installed, build Newclay with:

    cd compiler
    make

You can run the test suite with:

    python test/runtests.py

